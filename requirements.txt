# HackerBrain OS - dependencies
# Minimum versions. The pins are floor constraints so the stack installs
# cleanly on Python 3.10 through 3.13 (older exact pins fail to build on
# newer interpreters).
fastapi>=0.104.1
uvicorn>=0.24.0
websockets>=12.0
cryptography>=41.0.7
reportlab>=4.0.8
python-docx>=1.1.0
jinja2>=3.1.2
httpx>=0.25.2
pydantic>=2.5.2
python-multipart>=0.0.6
aiofiles>=23.2.1
